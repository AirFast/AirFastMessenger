import {} from '../actions/dialogActions';

const initState = {
};

const dialogReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default dialogReducer;